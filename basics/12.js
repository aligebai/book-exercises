this will evaluate to true because they are both strings
and the terminal does not treat them as numbers. Since
they are strings, Javascript makes the comparison from left
to right, and since '1' is lower than '9', it will return true
