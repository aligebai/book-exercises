Running this program will return 'Victor' for the variable
NAME in the first 3 greetings. But starting at line 6, the
program will return an error since NAME is a constant and cannot
be changed. If we want it to be changeable, we have to use let
instead of const.